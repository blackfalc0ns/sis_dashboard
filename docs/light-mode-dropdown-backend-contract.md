# LightModeDropdown Backend Contract

This file documents what the backend should provide so the frontend can connect `LightModeDropdown` to real data instead of the current default demo data.

## Current frontend source

- Component: `src/components/ui/dropdown/LightModeDropdown.tsx`
- Export: `src/components/ui/dropdown/index.ts`
- Current usage: `src/features/dashboard/views/SchoolDashboardView.tsx`
- The dashboard