import CommunicationPageFrame from "./CommunicationPageFrame";

export default function CreateConversationPage() {
  return (
    <CommunicationPageFrame
      title="New Conversation"
      subtitle="Conversation creation skeleton for participant selection, scope, title, validation, and submit states."
    />
  );
}
